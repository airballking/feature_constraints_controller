# actual constraint range
# NOTE: lower_boundary needs to be less than or equal to upper_boundary
float64 lower_boundary
float64 upper_boundary

# fraction of maximum velocity to be used
# NOTE: only values between 0.0 and 1.0 are valid
float64 maximum_velocity

# fraction of maximum acceleration to be used
# NOTE: only values between 0.0 and 1.0 are valid
float64 maximum_acceleration
