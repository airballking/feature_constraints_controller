Chain c;

c.getNrOfSegments()

  s = c.getSegment(i);
  Frame f = s.getFrameToTip()
  std::string name = s.getName();
  Joint j = s.getJoint();
  j.JointAxis()
  j.getType()

  // missing: scale, offset, origin
